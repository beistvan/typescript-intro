let guess;

console.log(`${typeof guess} - the type of guess`); // undefined

guess = "blue";

console.log(`${guess} - is the guess`);

guess = 42;

console.log(`${guess} - is the guess now`);

console.log(`${typeof guess} - the type of guess`); // number
